# factorial_recursion
it finds the factorial using a recursive function.

#seudocode

fact(n)
{
   if(n==1 || n==0)
   {
      factorial=1;
   }

  if(n>1)
  {
     factorial=(n)*fact(n-1);
   }
   
   
